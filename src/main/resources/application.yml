server:
  port: 809

spring:
  h2:
    console:
      enabled=true

  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: udxAtugEqSu@6bP
  jpa:

    open-in-view: false
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect=org:
          hibernate:
            dialect:
              PostgreSQLDialect:
